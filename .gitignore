.DS_Store
._*
.Spotlight-V100
.Trashes
*.swp
*~

# Project state (PAICodeConstitution-2026 Art. 5) — this repo has a remote,
# so none of this ever enters published history. CLAUDE.md/PROJECT_BRIEF.md
# reference the private workspace's internal governance structure and paths
# (caught before first push, 2026-09-15 — a sibling project in the same
# workspace had flagged identical "governance-path leakage" as a publish
# blocker; this repo is already public, so it mattered here immediately).
NEOCORTEX/
CLAUDE.md
PROJECT_BRIEF.md

# L1 enforcement adapter's local hook config (auto-generated by the private
# workspace's governance tooling) — gitignored per its own generated header.
.claude/
